the_foo.pootis();
let my_ass = "be free";

@Brotato("fooly", "cooly", (1, 2, 3))
@Big
class Foo

end

// How decorators decorate the world:
fn FooFighters()
  return fn(target, meta)
    println("Decorating class ", meta.name)
    return target
  end
end

let monoid = fn() true end

@FooFighters
fn foo_fighters()
  class bar end
  the_foo2;
  return brotato("code");
end
