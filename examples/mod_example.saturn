// This example illustrates how to use modules.
// Must compile with the flag --mod examples
use std;
use { foo } in com.bar;

let the_foo = foo();
for (k, v) in std.Object.entries(the_foo) {
  print(k, v);
}
