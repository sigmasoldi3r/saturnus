let { Object, operators: { `..` } } = use std;

let tbl = {
  a: 1,
  b: true,
  c: "x"
};

// For-each
for (k, v) in Object.entries(tbl) {
  print(k, v);
}

// Range based for
for i in 1..4 {
  print("integer", i);
}
for i in 0..(1, 0.1) {
  print("decimal", i);
}
