// This example demonstrates the use of the STD filesystem
use { fs } in std;

// Write simple file!
fs::write_file("hello.txt", "Hello World!");

// Now read it:
let content = fs::read_file("hello.txt");

print("Content:", content);
