// Example of OOP possible uses with std
let { abstract, entries } = require("../src/assets/std");

class Simple {}

class TraitLike {
  @abstract() fn foo(self) {}
}

let a = TraitLike {};
a.foo();

let x = [1, 2, 3];
let y = [4, 5, 6];
let z = x ::: y;

for (k, v) in entries(z) {
  print(k ++ " => " ++ v);
}
