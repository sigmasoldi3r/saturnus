// Destructuring assignment
use { Object } in std;

let { foo, bar } = something;
let ( x, y ) = some_other;
let [ one, two ] = some_more;

for (k, v) in Object.entries(foo) {
  print(k ++ " => " ++ v);
}

