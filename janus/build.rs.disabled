use std::{fs, path::PathBuf, process::Command};

fn main() {
    Command::new("cargo")
        .arg("build")
        .current_dir("../runtime")
        .status()
        .unwrap();
    // let base = PathBuf::from("..")
    //     .join("runtime")
    //     .join("target")
    //     .join("release");
    // let bin = fs::read(base.join("runtime"))
    //     .unwrap_or_else(|_| fs::read(base.join("runtime.exe")).unwrap());
}
