use crate::{compilation::CompilationInfo, janusfile::DependencyList};

pub fn resolve_deps(info: &CompilationInfo, dependencies: DependencyList) {
    println!(" Resolving dependencies...");
    println!(" Done");
}
